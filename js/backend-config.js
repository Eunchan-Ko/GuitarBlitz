/* ===================================================================
   백엔드 설정 — Supabase 프로젝트 정보를 여기에 넣습니다.
   비워두면 랭킹이 이 브라우저의 localStorage 에만 저장됩니다.
   (게임은 설정 없이도 전부 정상 동작합니다)

   값은 Supabase 대시보드 > Project Settings > API 에서 확인합니다.
   anon key 는 공개되도록 설계된 값이라 코드에 넣어도 됩니다.
   다만 supabase/schema.sql 의 RLS 정책을 반드시 함께 적용해야 합니다.
   =================================================================== */

const BACKEND_CONFIG = {
    supabaseUrl: '',
    supabaseAnonKey: ''
};
